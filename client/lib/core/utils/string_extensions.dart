/// Расширения для работы со строками в тактическом интерфейсе.
extension TacticalString on String {
  /// Преобразует строку в верхний регистр для единообразия UI.
  ///
  /// Позволяет избежать замусоривания UI-кода вызовами .toUpperCase().
  String get t => toUpperCase();
}
