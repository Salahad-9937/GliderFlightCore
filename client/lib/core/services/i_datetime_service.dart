/// Интерфейс службы времени для обеспечения детерминизма.
///
/// Позволяет фиксировать время в тестах.
abstract interface class IDateTimeService {
  /// Возвращает текущий момент времени.
  DateTime now();
}
